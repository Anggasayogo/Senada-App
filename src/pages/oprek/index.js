import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import {Headers} from '../../components';

const Oprek = () => {
    return (
        <View>
            <Headers height={60}/>
            <Text>Oprek</Text>
        </View>
    )
}

export default Oprek

const styles = StyleSheet.create({})
